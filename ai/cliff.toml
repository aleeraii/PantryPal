# git-cliff configuration for the PantryPal AI agents
# Generates RELEASE.md from commits scoped to "ai"

[changelog]
header = """
# Changelog — AI

All notable changes to the PantryPal AI agents are documented here.
This file is auto-generated by [git-cliff](https://github.com/orhun/git-cliff).

"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}
- {% if commit.breaking %}[**BREAKING**] {% endif %}{{ commit.message | upper_first }} ([{{ commit.id | truncate(length=7, end="") }}]({{ commit.id }}))\
{% endfor %}
{% endfor %}\n
"""
footer = ""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_preprocessors = []

commit_parsers = [
    { message = "^feat\\(ai\\)", group = "Features" },
    { message = "^fix\\(ai\\)", group = "Bug Fixes" },
    { message = "^perf\\(ai\\)", group = "Performance Improvements" },
    { message = "^refactor\\(ai\\)", group = "Refactoring" },
    { message = "^revert\\(ai\\)", group = "Reverts" },
    { message = "^docs\\(ai\\)", skip = true },
    { message = "^style\\(ai\\)", skip = true },
    { message = "^test\\(ai\\)", skip = true },
    { message = "^chore\\(ai\\)", skip = true },
    { message = "^ci\\(ai\\)", skip = true },
    { message = "^build\\(ai\\)", skip = true },
    { message = ".*", skip = true },
]

filter_commits = false
tag_pattern = "ai-v[0-9].*"
skip_tags = ""
ignore_tags = ""
topo_order = false
sort_commits = "oldest"
